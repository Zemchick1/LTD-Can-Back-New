input{
    jdbc{
        jdbc_driver_library => "./bin/postgresql-42.7.0.jar"
        jdbc_driver_class => "org.postgresql.Driver"
        jdbc_connection_string => "jdbc:postgresql://postgres:55433/book_service"
        jdbc_user => "Shapiro"
        jdbc_password => "Sardelka0911"
        jdbc_paging_enabled => true
        statement => "SELECT *"
    }
}

filter{
}

output{
    stdout { codec =>  "rubydebug"}
}